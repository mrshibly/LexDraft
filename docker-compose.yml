version: '3.8'

services:
  lexdraft:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./sample_outputs:/app/sample_outputs
    restart: always
